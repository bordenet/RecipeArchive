# RecipeArchive Environment Configuration Template
# Copy this file to .env and replace with your actual values
# NEVER commit the actual .env file to version control

# IMPORTANT: Development requires active AWS account
# Extensions connect directly to AWS Cognito and S3
# Expect ~$1-5/month in AWS costs during development

# AWS Cognito Test User Credentials (for end-to-end testing)
# Create a test user in AWS Cognito and add credentials here
TEST_USER_EMAIL=your-test-email@example.com
TEST_USER_PASSWORD=YourSecurePassword123

# Recipe Report Tool Credentials (for validation script)
# Used by validate-monorepo.sh for final health check
RECIPE_USER_EMAIL=your-email@example.com
RECIPE_USER_PASSWORD=YourPassword123

# AWS Configuration (REQUIRED)
AWS_REGION=us-west-2
COGNITO_USER_POOL_ID=your-cognito-user-pool-id
COGNITO_APP_CLIENT_ID=your-cognito-app-client-id

# S3 Configuration (REQUIRED - matches CDK deployment variable names)
# Replace with your actual S3 bucket names from CDK deployment
S3_STORAGE_BUCKET=your-s3-storage-bucket-name
S3_TEMP_BUCKET=your-s3-temp-bucket-name

# DynamoDB Configuration (deprecated - using S3 storage)
DYNAMODB_TABLE_NAME=your-dynamodb-table-name